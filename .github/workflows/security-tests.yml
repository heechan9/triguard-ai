name: General upload security tests
on: [push, pull_request, workflow_dispatch]
permissions:
  contents: read
jobs:
  upload-security:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m unittest discover -s tests -p 'test_upload_security.py' -v
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node --test tests/web-security.test.cjs
      - run: python scripts/build_public_catalog.py
      - run: python -m unittest discover -s tests -p 'test_public_catalog.py' -v
      - run: node --check public-data-demo/app.js
